// Shared constants needed by both the recharts chunk and the recharts-free
// shell/bits (skeleton heights must match the real chart heights — CLS guard).
export const PORTAL_CHART_HEIGHT = 380
export const SNAPSHOT_CHART_HEIGHT = 360
export const SPARK_HEIGHT = 56
